services:
  - type: web
    name: assignflow
    env: node
    plan: free
    buildCommand: echo "Build skipped"
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: SESSION_SECRET
        generateValue: true
